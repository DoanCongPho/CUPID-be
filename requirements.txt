# pyproject.toml (Poetry) is the source of truth for dependencies.
# This file is for pip / build.sh (deploy) and must be kept in sync with it.
# Only direct dependencies are listed; pip resolves transitive ones.
# These versions are a verified working set (migrate, server and test suite all pass).

# --- Core Django ---
Django==5.2.17
djangorestframework==3.18.1
python-dotenv==1.2.3
dj-database-url==3.1.2
drf-spectacular==0.29.0

# --- Realtime / WebSocket (Channels) ---
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.3
uvicorn==0.38.0
websockets==15.0.1

# --- Database / Cache ---
psycopg2-binary==2.9.13
django-redis==6.0.0

# --- Auth ---
python-jose==3.5.0
cachetools==6.2.6

# --- Matching engine (engine.py, engine_gen_quest.py) ---
numpy==2.5.3
scipy==1.18.1

# --- External services ---
supabase==2.31.0
requests==2.34.2

# --- Deploy ---
gunicorn==23.0.0
whitenoise==6.11.0

# --- Dev tools (not needed in production) ---
black==25.9.0
isort==7.0.0
autopep8==2.3.2
